# Canonical upstream integration — DD-000

The Shared Discovery Paradox repository is canonical and read-only. `upstream.lock` is populated by M1 after fetching into ignored `.cache/upstream/shared-discovery-paradox`. Store only provenance, additive fragments, and reviewable patches here. Never push upstream from this workspace.
