# DD-C-0080 verification — 2026-07-22

Result: passed as a `verified` bounded exact computational result.

The common-information DP exhausts 27 prescriptions at every reachable public
state and uses exact fractions. A separate labeled policy-tree enumerator sums
all target, shared-clue, and private-clue paths, checks probability mass one,
and reproduces discovery and expected actions for both protocols on all 64
objective rows. It performs 128 protocol checks and four corruption gates.

The run confirms 38 strict planner gains, 32 rows with positive previous-action
following, 50 with positive leaning against a repeated prior action, and fewer
expected actions under stopping in all 32 parameter cells. These counts are
bounded, not general theorems.

Commands: `pytest tests/unit/test_dynamic_attention.py
tests/integration/test_dynamic_attention_cli.py`; `make validate-claims`.
