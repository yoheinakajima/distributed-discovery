# DD-C-0063 verification — 2026-07-21

Result: passed; status `verified` for the registered symmetric-garbling family.

For positive audience `m` and delivered accuracy `g`, failure is
`(1-g)(1-p)^(N-m)`. When `q>p`, both factors are weakly larger than under
full-precision one-recipient delivery, with equality only at `g=q,m=1`. At
`q=p`, that design also ties no delivery. When `q<p`, every `g<=q` is already
worse at one recipient and additional recipients strictly reduce discovery.

The immutable census contains 2,625 feasible garbling rows. Exactly 105 equal
the binding optimum; all are full-precision one-recipient designs in `q>=p`
cells. The other 2,520 are strictly dominated. Direct enumeration reproduces
every row and rejects an altered dominance flag. Nothing here orders arbitrary
information experiments.
