# DD-C-0090 verification — 2026-07-22

Result: passed as an `independently-reproduced` exact bounded computational
counterexample. Exact calculation gives both registered channels one-person
accuracy `1/2` and direct private discovery `7/8`, while two exhaustive methods
give unequal profiles `(7/12,43/54,25/27)` and `(17/18,1,1)`. The statement is
limited to insufficiency of one-person accuracy/expected posterior maximum.

Commands: `pytest tests/unit/test_signal_geometry.py`; `make validate-claims`.
