# Claim check — DD-C-0038

- Date: 2026-07-20.
- Outcome: pass; status **independently reproduced**.
- Primary upper-bound run: **20260721T022739Z_DD-001_358cb1eb_cd16846ba5**.
- Direct-policy evidence run: **20260720T200447Z_DD-001_6eb12861_ba766d1eba**.

The feasible direct profile independently gives exact discovery
`325089/390625`. The audited alignment relaxation returns the same upper bound,
with target-resource witness `(8,...,8)` across all sixteen targets and exact
average failure `65536/390625`. Its 102,087-byte certificate passes the separate
Bellman verifier and corruption test. Equality of a feasible lower bound and a
valid global upper bound proves the deterministic optimum.

The randomized extension was checked separately: an ex-ante seed induces a
distribution over deterministic profiles, so expected discovery is their convex
combination and cannot exceed the deterministic maximum. No assumption about
unrestricted communication, other dimensions, or other signal laws is imported.
