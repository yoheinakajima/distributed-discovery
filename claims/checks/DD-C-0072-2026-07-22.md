# DD-C-0072 verification — 2026-07-22

Result: passed as a `verified` exact identity.

Conditioning on the binomial count of other agents at candidate j gives the
finite equal-split sum directly. The coefficient identity
`binom(N-1,k)/(k+1)=binom(N,k+1)/N` yields the tau-one expression. Removing its
k=0 term yields the tau-two expression. At x=0 the direct finite sums give
share factors one and zero respectively, agreeing with the continuous limits.

The immutable run records 35 exact rational comparisons for N=2 through 8 and
x in `{0,1/8,1/3,1/2,1}`. Every direct sum equals its closed form. The scope is
a conditional payoff calculation; no equilibrium selection is inferred.

Commands: `uv run pytest tests/unit/test_threshold_discovery.py`; `uv run
python -m distributed_discovery.validation.runs`; `make validate-claims`.

