# DD-C-0095 verification — 2026-07-22

Result: passed as an `independently-reproduced` exact bounded computational
result. Full count-vector enumeration and a correct-count/wrong-occupancy
dynamic program agree on all 2,555 rows. The clean immutable run records 1,848
negative and 196 perfect-signal zero increments, no positives, all endpoints,
bounds, exact row count, and five rejected corruptions.
The post-run audit independently confirms exact probability normalization and
pooled-MAP tie probability for every distinct registered `M,p,s` combination;
it does not mutate or rerun the primary configuration.

Commands: `pytest tests/unit/test_incremental_sharing.py
tests/unit/test_incremental_sharing_post_run_audit.py`; `make
validate-claims`; `python -m distributed_discovery.validation.runs`.
