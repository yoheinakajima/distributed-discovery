# DD-C-0076 verification — 2026-07-22

Result: passed as a scoped `verified` theorem.

Uniform mixing over m posterior modes makes all support payoffs equal. For
tau at least two, a deviation to a zero-probability outside action attracts no
other agent and cannot open, while every support action has positive expected
payoff. For tau one, the outside action opens alone and pays its posterior;
comparison with the exact split-prize support factor gives the stated necessary
and sufficient condition.

Direct opponent-profile enumeration reproduces every analytic action payoff in
the 160-game registry. All 21 registered failures occur at tau one. In the
hand-checkable N=2 fixture `(7/20,7/20,3/10)`, support payoff `21/80` is below
outside payoff `3/10`. This verifies one mixture, not all mixed equilibria or a
selection process.

Commands: `uv run pytest tests/unit/test_threshold_equilibrium.py
tests/integration/test_threshold_equilibrium_cli.py`; `make validate-claims`.

