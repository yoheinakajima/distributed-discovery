# DD-C-0071 verification — 2026-07-22

Result: passed as a scoped `verified` theorem.

The capacity argument shows that opening q candidates consumes at least q*tau
agents, so q cannot exceed `floor(N/tau)` or M. Posterior ordering bounds every
opened set by the top `min(floor(N/tau),M)` masses. Assigning exactly tau agents
to those candidates attains the bound; leftovers cannot close a candidate or
form an additional team. This proves existence and value, not uniqueness.

The proof was reviewed separately from the implementation. Run
`20260722T021526Z_DD-016_00271ff8_123b2809e3` records 63 exact exhaustive
comparisons over three posterior fixtures, N=1 through 6, and all admissible
thresholds. Every formula value equals the maximum over every weak composition
of agents across candidates.

Commands: `uv run pytest tests/unit/test_threshold_discovery.py`; `uv run
python -m distributed_discovery.validation.runs`; `make validate-claims`.

