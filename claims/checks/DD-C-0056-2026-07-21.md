# DD-C-0056 verification — 2026-07-21

Result: passed for the frozen DD-011 synthetic design and power grid; status is
`verified` because every Monte Carlo row is a conditional estimate.

Primary run `20260721T185647Z_DD-011_fa0271d9_fcaa647c55` records clean source
commit `fa0271d93376004a3a88aab29710518b25c2ffae`, configuration hash
`fcaa647c55efa3602d6bace7557892f1f4afc8b2c55632f74b2be1da026643f9`,
the dependency lock, six input hashes, one assignment seed, eight scenario seeds,
one sample seed, and fourteen checksum-bound JSON outputs.

The run generates 640 balanced synthetic assignments across 20 treatment cells.
It evaluates eight frozen hypotheses under eight response scenarios and six total
sample sizes using 1,000 replications per row, for 384,000 draws. A separate
implementation recreates every row's random stream, effect, critical value, and
rejection count. It validates all 384 rows, the schema, cell/estimand references,
assignment balance, and four exact source fixtures. Corruption tests reject an
altered rejection count, a non-synthetic identifier, and a removed no-human-data
boundary.

Under scenario S1 (full rational best response), every registered assumed effect
reaches estimated power at least 0.80 by total sample size 960; seven do so by 640.
Across the 192 scenario/hypothesis rows at sample sizes 640, 800, or 960, 140 stay
below 0.80 and are retained rather than hidden. These counts are properties of the
fixed simulation. They are not evidence about participant behavior or recruitment.

Commands: `make dd011-experiment`; `distributed-discovery experiment verify
20260721T185647Z_DD-011_fa0271d9_fcaa647c55`; `make verify`; `make site`.

No participants were recruited. No human data were collected. No experiment was
conducted. Separate ethics and institutional review are required before deployment.
