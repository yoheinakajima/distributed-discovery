# DD-C-0079 verification — 2026-07-22

Result: passed as a scoped `verified` proposition.

Full duplicate credit makes an agent's payoff from action `a` exactly its
posterior probability that `a` is the target. Later actions cannot dilute or
increase that payoff, so a posterior mode is a best response at every reached
information set. The proof and implementation retain the lowest-label tie rule
and distinguish fixed-budget action evidence from stopping failure evidence.

Primary run `20260722T043713Z_DD-015_92d53ac1_0e7cf1ec0a` normalizes every
labeled path and checks every resulting autonomous policy tree. The claim does
not extend to the reward rules that create congestion or team complementarities.

Commands: `pytest tests/unit/test_dynamic_attention.py`; `make
validate-claims`.
