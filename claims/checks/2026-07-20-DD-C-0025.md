# Claim check — DD-C-0025

- Date: 2026-07-20
- Outcome: pass; status `verified` is supported for the exact finite counts.
- Evidence run: `20260720T221139Z_DD-001_b1d8d431_40bf5b06a5`.

Reviewed the labeled-signature count by conditioning on the nonfixed target set and subtracting exactly the singleton Hall obstructions. The target-orbit dynamic program counts bounded residual multisets separately for fixed and nonfixed targets; exhaustive enumeration agrees through $M=5$. The agent-multiset total is the exact binomial count using all labeled signatures.

The separate certificate checker recomputes the raw-policy, signature, orbit, agent-multiset, and minimum-memory integers and detects a deliberately corrupted signature count in unit tests. The run explicitly records that independent per-agent orbit canonicalization is not lossless and that the canonical optimum remains unresolved.

Commands: independent certificate verifier on `reduction-certificate.json`; corruption-detection unit test; targeted signature tests; run-manifest validation; `make validate-claims`. All passed. The count is not described as a general algorithmic lower bound or an objective certificate.
