# DD-C-0069 verification — 2026-07-21

Result: passed for the registered DiscoveryBench v2 selective-attention
extension; status recorded as `independently-reproduced`.

Clean primary run `20260721T230249Z_DD-010_add85590_56c61a2195` records source
commit `add8559016820c3592a906ca9d662cdfcbda8a5c`, configuration hash
`56c61a219515ab718699d5e5578d47a1863a48299c9303c29ecd919107e3ca85`,
the dependency lock, six executable/configuration input hashes, declared seeds,
and thirteen checksum-addressed JSON outputs. Runtime was 0.63 seconds inside
the 60-second cap, and `git_dirty` is false.

All 20 v2 tasks pass the v2 JSON Schema and semantic checks. The evaluator
classifies all 420 task/protocol candidates, runs 28 compatible pairs, and
retains 392 explicit exclusions. A materially separate verifier derives the
attention fixtures at `N=4`, `p=1/2`, and `q=3/4` from exact rational
identities and checked DD-012--DD-014 results. It confirms private-only
discovery `15/16`, designated-reader discovery `31/32`, voluntary-equilibrium
discovery `7/8`, public-only discovery `3/4`, and pure-contrarian discovery
`895/1024`, together with their counts, gains, losses, wedges, audience sizes,
and categories.

The v1 CLI default remains 15 tasks, 13 protocols, 19 metrics, 16 compatible
pairs, and 179 exclusions. Tests compare every v1 task/protocol metric vector
against the v2 prefix. Capability views reject hidden evaluator/provenance
fields, and the corruption audit rejects an altered value, target-state access,
and an altered compatibility count.

Commands: `make dd010-attention`; `distributed-discovery benchmark --version
v2 verify-run results/verified/20260721T230249Z_DD-010_add85590_56c61a2195`;
`make verify`; `make site`. The unchanged eight-seed sensitivity suite remains
an estimated calibration diagnostic outside this exact claim.
