# DD-C-0074 verification — 2026-07-22

Result: passed as an `independently-reproduced` bounded phase census.

Both state representations reproduce all eight exact threshold rows. Direct
fraction comparisons show that common deterministic mode and the registered
tied-mode mixture are below private clue-following only at tau one and two,
then above it at tau three through eight. The planner is strictly above the
selected mixture in every row. The floor capacity permits only one viable team
at tau five through eight, where the planner and common-mode values tie; strict
planner diversification gains occur at tau one through four.

All row labels are determined from exact fractions. The verifier also checks
normalization, posterior-mass accounting, planner and occupancy bounds, 63
small exhaustive planner cases, 35 strategic payoff cases, and all four
corruption rejections. No categorical label relies on a rounded decimal.

Commands: `uv run pytest tests/unit/test_threshold_discovery.py
tests/integration/test_threshold_discovery_cli.py`; `uv run python -m
distributed_discovery.validation.runs`; `make validate-claims`.
