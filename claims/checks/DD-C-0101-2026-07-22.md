# DD-C-0101 verification — 2026-07-22

Result: passed as an `independently-reproduced` exact bounded witness claim.
Both methods agree on the minimized paradox, strict consensus-dominance, and
nonperfect no-useful-one-action witnesses and their exact inequalities. The
independent verifier searches the complete candidate order and the post-run
audit confirms the immutable registry hashes and class counts.

Commands: `pytest tests/unit/test_general_sharing.py
tests/unit/test_general_sharing_post_run_audit.py`; `make validate-claims`.
