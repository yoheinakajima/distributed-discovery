# Verification check: DD-C-0068

- Claim: unrestricted-policy scope counterexample
- Outcome: pass as `independently-reproduced` negative result
- Evidence: all 1,024 ordered raw profiles across four cells are exhausted; complementary constants attain discovery one in every cell and strictly beat the embedded private/public optimum.
- Independent audit: a separate raw table evaluator reproduces 16,384 labeled states, equilibrium sets, and exact optima.
- Corruption audit: an altered raw optimum is rejected.
- Boundary: two labels and two agents; this prevents unrestricted promotion but is not a complete unrestricted `M=3` result.
