# DD-C-0078 verification — 2026-07-22

Result: passed as an `independently-reproduced` bounded computational result.

For every pure Nash occupancy, the primary evaluator enumerates named
coalitions and joint action assignments. The independent verifier instead
enumerates removal and addition occupancy vectors and checks for a perfect
matching from origin payoffs to strictly better destination payoffs. The two
representations agree on every pairwise and exact-size-tau stability label.

Eight registered games have no pairwise-strict-stable weak pure Nash
equilibrium; 35 have no tau-player-strict-stable weak pure Nash equilibrium.
The labels require every coalition member to improve strictly and do not imply
strong or coalition-proof equilibrium. An altered pair-stability label is
rejected, and hand-checkable three- and four-agent threshold-two fixtures pass.

Commands: `uv run pytest tests/unit/test_threshold_equilibrium.py
tests/integration/test_threshold_equilibrium_cli.py`; `make validate-claims`.
