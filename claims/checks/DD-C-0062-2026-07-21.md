# DD-C-0062 verification — 2026-07-21

Result: passed; status `verified` as the Binding Audience Theorem.

With `r=1-p`, no delivery gives discovery `1-r^N`; a positive binding audience
of size `m` gives `1-(1-q)r^(N-m)`. The first delivery margin is
`r^(N-1)(q-p)`, and every later margin is strictly negative. This proves the
full audience-optimum correspondence for arbitrary finite `N>=2` and interior
accuracies inside the frozen action class.

Clean run `20260721T215811Z_DD-013_09c07448_cdac4fb512` records source commit
`09c0744864697a340f01afd7e9ae68dd60aba061`. Its separate labeled-state
enumerator reproduces discovery, action quality, and expected distinct actions
for all 1,050 binding rows and rejects altered binding discovery. The proof,
not the finite grid, supports the general finite-`N` quantifier.
