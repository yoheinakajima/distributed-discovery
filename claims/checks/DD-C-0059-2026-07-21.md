# DD-C-0059 verification — 2026-07-21

Result: passed; status `verified` as a theorem for the frozen follow/private
policy class.

The proof in `studies/DD-012-incentive-to-ignore/proof.md` derives discovery
from its failure event. With `r=1-p`, zero readers give `1-r^N`, while any
positive reader count `k` gives `1-(1-q)r^(N-k)`. The first-reader margin is
`r^(N-1)(q-p)` and every subsequent margin is
`-(1-q)p r^(N-k-1)<0`. These signs prove the stated complete optimum
correspondence for every finite `N>=2` and interior `p,q`.

Clean run `20260721T212943Z_DD-012_9ed0928e_4a3f1ba62b` records source commit
`9ed0928e5aacc7b1786fb0c8754a3d03db133ae1`, validates all 175 rational cells,
and separately enumerates target, shared-signal, and action-relevant private
signals for all 1,050 profiles. The finite audit checks the formula; the proof,
not grid extrapolation, supports the general finite-`N` quantifier.
