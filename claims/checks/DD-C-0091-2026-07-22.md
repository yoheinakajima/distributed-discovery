# DD-C-0091 verification — 2026-07-22

Result: passed as an `independently-reproduced` exact bounded computational
result. Exact sequential comparisons of each independently reproduced profile
with its named private baseline give recovery budgets `3,2,1,2,3`. The
corruption gate confirms the point channel cannot recover at `L=1`.

Commands: `pytest tests/unit/test_signal_geometry.py`; `make validate-claims`.
