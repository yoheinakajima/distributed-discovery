# DD-C-0058 verification — 2026-07-21

Result: passed; status `independently-reproduced` as an exact negative result.

At `N=3`, `p=4/5`, the human-readable derivation gives
`A_1=p(1-p)(3-2p)/6=14/375` and
`B_1=p(1-p)^2=4/125=12/375`. Choosing `c=13/375` therefore gives the strict
ordering `A_1>c>B_1`. The threshold characterization returns unique equilibrium
count `k=2` and unique planner count `k=1`.

The primary DD-008B run recomputes the deviation payoffs by separate exhaustive
target/source-signal enumeration and records the same rational thresholds and
counts. The result refutes only a universal under-acquisition ordering. It does
not establish prevalence, empirical over-acquisition, or robustness outside the
frozen model.
