# DD-C-0055 verification — 2026-07-21

Result: passed for DiscoveryBench v1's registered golden suite; status promoted
to `independently-reproduced`.

Primary run `20260721T183014Z_DD-010_ce930050_8ec718c242` records clean source
commit `ce930050561e6295209589a7ef5a9a555aa6b975`, configuration hash
`8ec718c242d76b1b7cc6d9667f76fb53c4e0d56f2e41007c54d2a447c38e016a`,
the dependency lock and six executable/configuration input hashes, explicit
simulation seeds, and thirteen checksum-valid JSON outputs.

All fifteen task contracts pass the versioned JSON Schema and semantic checks.
The evaluator classifies all 195 task/protocol candidates, runs all sixteen
declared compatible pairs, and retains 179 exact exclusions. A separate module
recomputes all sixteen metric vectors using rational identities and checked
fixture values, resolves every source claim and immutable run, and verifies the
aggregate counts. Protocols receive immutable capability views. Attempts to
read target state, peer-private signals, future outcomes, undeclared sources,
global evaluator state, expected metrics, or provenance are rejected. The
verifier also rejects one altered discovery value and one leaked target
capability.

Only metric vectors with declared observables are emitted. Family profiles and
scoped Pareto rows are published without a composite score. The subsequent
seeded sensitivity suite uses eight declared seeds and 1,000 Bernoulli draws per
seed; its mean `0.65737500` is a simulation estimate and is not part of the exact
claim.

Commands: `make dd010-discoverybench`; `distributed-discovery benchmark
verify-run results/verified/20260721T183014Z_DD-010_ce930050_8ec718c242`;
`make verify`; `make site`. The result does not validate a hosted leaderboard,
unregistered protocol, external model, real-world system, or universal ranking.
