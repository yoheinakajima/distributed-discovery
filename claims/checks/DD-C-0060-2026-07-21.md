# DD-C-0060 verification — 2026-07-21

Result: passed; status `verified` as an equal-split pure-equilibrium theorem.

The written derivation obtains exact attender and non-attender payoffs and
checks `k A_N(k)+(N-k)I_N(k)=G_N(k)`. It reduces one role's attention gain to

`Delta_k=q(1-p)E[1/(k+1+Z_k)]-p(1-q)E[1/(1+Z_k)]`.

A Bernoulli coupling makes the first expectation strictly fall and the second
strictly rise with `k`, proving that `Delta_k` strictly decreases. Adjacent
deviation inequalities therefore characterize all weak and strict pure counts.
The boundary identities give all-ignore iff `q<=p` and all-attend iff
`q>=Np/(1+(N-1)p)`.

The primary run's independent labeled-state evaluator reproduces every type
payoff and deviation at all 1,050 profiles. Its theorem grid confirms strict
margin decrease and rejects a flipped equilibrium flag. The result concerns
ex-ante non-delivery and expected prize incentives, not behavioral avoidance.
