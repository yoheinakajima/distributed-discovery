# DD-C-0096 verification — 2026-07-22

Result: passed as an `independently-reproduced` exact bounded computational
counterexample. Labeled signal-sequence enumeration and signal-histogram
enumeration agree on all five profiles. The two named channels both have
one-person accuracy `1/2` but exact profiles `(7/8,3/4,7/12)` and
`(7/8,11/12,17/18)`, with opposite strict increment signs.

Commands: `pytest tests/unit/test_incremental_sharing.py
tests/unit/test_signal_geometry.py`; `make validate-claims`.
