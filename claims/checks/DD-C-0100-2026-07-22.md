# DD-C-0100 verification — 2026-07-22

Result: passed as an `independently-reproduced` exact bounded witness claim.
Both exhaustive methods apply the frozen lexicographic order and agree on the
`M=4,N=2` point/guaranteed-shortlist pair and increments `-1/4` and `1/12`.
The smaller `M=3,N=2` pair is negative versus neutral, so it cannot satisfy
the registered strict opposite-sign predicate.

Commands: `pytest tests/unit/test_general_sharing.py
tests/unit/test_general_sharing_post_run_audit.py`; `make validate-claims`.
