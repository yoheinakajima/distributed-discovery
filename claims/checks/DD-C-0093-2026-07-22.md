# DD-C-0093 verification — 2026-07-22

Result: passed as a `verified` theorem. Exchangeability and the declared tie
rule prove `C_2=p`; the identity then gives the exact discovery loss. The
separate proof audit checks the informative, uninformative, and perfect-signal
boundaries. Both exact implementations reproduce every registered `N=2` row.

Commands: `pytest tests/unit/test_incremental_sharing.py`; `make
validate-claims`.

