# DD-C-0099 verification — 2026-07-22

Result: passed as an `independently-reproduced` exact bounded computational
result. Ordered-profile and histogram/count-state enumeration agree on all 177
rows and class labels. The clean immutable run records 936,063 versus 117,433
audited states, output hashes, full summaries, and eight rejected corruptions.
The post-run audit independently recomputes identities and count tables from
the immutable JSON without rerunning the primary configuration.

Commands: `pytest tests/unit/test_general_sharing.py
tests/unit/test_general_sharing_post_run_audit.py
tests/integration/test_general_sharing_cli.py`; `make validate-claims`.
