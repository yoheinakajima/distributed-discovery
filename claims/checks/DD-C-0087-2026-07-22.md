# DD-C-0087 verification — 2026-07-22

Result: passed as an `independently-reproduced` exact computational result.

The immutable v3 run validates 24 task contracts, 29 capability-declared
protocols, 39 task-level metrics, all 696 candidate pairs, 36 exact compatible
rows, and 660 explicit exclusions. The separate verifier reconstructs every
metric vector, resolves each claim and run reference, checks schema/count and
information boundaries, and rejects value, compatibility, and target-state
leakage corruptions.

The first 28 v3 task/protocol/metric vectors exactly match the complete v2
matrix; v1 remains the CLI default. No composite score or enabled external
adapter exists. The eight-seed synthetic sensitivity artifact is unchanged
calibration and does not support this exact claim.

Commands: `pytest tests/unit/test_discoverybench.py
tests/integration/test_discoverybench_cli.py`; `make validate-claims`.
