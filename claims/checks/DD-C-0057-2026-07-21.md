# DD-C-0057 verification — 2026-07-21

Result: passed; status `verified` as a general finite-`N` theorem.

The proof in `studies/DD-008B-common-source-analysis/proof.md` derives the gross
discovery and both type-payoff expressions, then defines the private deviation
threshold

`A_k=p(1-p)E[1/(1+X_k)-1/(N-k+X_k)]`.

A Bernoulli coupling reduces `A_k-A_(k+1)` to an expectation of a rational
kernel with positive denominator and numerator
`(x+1)(x+2)+p(m-2)(2x+m+1)`. This proves strict threshold monotonicity rather
than inferring it from a finite grid. The same proof derives planner thresholds,
the complete weak count characterizations, and the all-common trap interval of
exact width `p(1-p)/N`.

Primary clean run `20260721T192412Z_DD-008B_649deb08_29dbeaf3a9` records source
commit `649deb08bd2fcbaa88e22a6078a4a9d2e1e0b011` and configuration hash
`29dbeaf3a989ac8062e882bf4163e0123c9c11b134e5ae99aef68c066cad4fe5`.
It reproduces all 126 immutable DD-008A classifications, independently checks
84 direct payoff margins through `N=5`, and instantiates 16,368 positive-kernel
identities through `N=32`. Altered thresholds and altered frozen-census
classifications are rejected. Finite computation audits the formulas but is not
the basis for the unrestricted finite-`N` quantifier.
