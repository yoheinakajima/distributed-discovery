# DD-C-0064 verification — 2026-07-21

Result: passed; status `verified` as a scoped implementation theorem.

Under universal pooling each role receives `1/N` exactly on team discovery, so
expected payoff is `G_N(k)/N`. Every unilateral ex-ante mode change therefore
has the same sign as the corresponding discovery change. DD-C-0062's shape
gives weak equilibrium counts `{1}` for `q>p`, `{0}` for `q<p`, and `{0,1}`
at equality; implementation is strict off equality. Realized transfers sum to
the unit discovery prize in successful states and zero otherwise, establishing
ex-post budget balance with no subsidy.

The direct payoff evaluator reconstructs the pooling deviations in all 175
cells and rejects an altered mechanism correspondence. Reader identity remains
an `N`-way symmetry when one reader is optimal; assignment or rotation is a
separate identity-selection institution.
