# DD-C-0073 verification — 2026-07-22

Result: passed as an `independently-reproduced` bounded computational result.

Clean run `20260722T021526Z_DD-016_00271ff8_123b2809e3` records source commit
`00271ff8fbcb10012c2ae11e256a772662ea5f2b`, configuration hash
`123b2809e3a495c81d877164948d31cd936401eaa126ab40b1baee6d0ee41f55`,
the dependency lock, five input hashes, no random seeds, and seven
checksum-addressed outputs. Runtime was 11.998247 seconds inside the
600-second cap, and `git_dirty` is false.

Method A exhausts 490,314 labeled clue-count vectors. Method B instead
enumerates 67 false-label occupancy orbits. They reconstruct the same 30 signal
classes, normalize to one, and agree on every exact row. The tau-two fractions
in the ledger are read from the saved output, not from supplied regression
decimals. The methods share the frozen metric definitions but independently
construct state weights and aggregate signal classes.

The verifier rejects altered posterior weight, occupancy probability, mode
count, and planner mass. Smaller planner, payoff, and occupancy fixtures pass.
The tied-mode result is explicitly one independent-uniform selection rule, not
the full strategic equilibrium correspondence.

Commands: `uv run pytest tests/unit/test_threshold_discovery.py
tests/integration/test_threshold_discovery_cli.py`; `uv run python -m
distributed_discovery.validation.runs`; `make validate-claims`.

