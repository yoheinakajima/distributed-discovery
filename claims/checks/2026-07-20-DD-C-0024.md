# Claim check — DD-C-0024

- Date: 2026-07-20
- Outcome: pass; status `independently-reproduced` is supported.
- Primary signature run: `20260720T221139Z_DD-001_b1d8d431_40bf5b06a5`.
- Prior raw-policy run: `20260720T200447Z_DD-001_6eb12861_ba766d1eba`.

The primary run contains 21 rows. Every signature optimum matches the prior raw-policy optimum, the reconstructed raw formula, direct target/signal enumeration, the Fraction reference signature evaluator, and the scaled-integer evaluator. Probability mass normalizes exactly. Signature multiplicities reconcile the prior raw-policy multiset tie count in every row, including six ties at $(M,N,p)=(3,2,2/5)$. Direct and territorial benchmarks and the pooled-emulation relation also pass.

The signature representation, feasibility search, and integer objective are materially distinct from the prior raw-policy enumeration. Both run manifests are clean and their output hashes validate. Scope remains the frozen grid; no continuous threshold or canonical optimum is inferred.

Commands: `make dd001-signatures`; targeted raw/signature unit tests; independent certificate verifier; run-manifest validation; exact CSV audit; `make validate-claims`. All passed.
