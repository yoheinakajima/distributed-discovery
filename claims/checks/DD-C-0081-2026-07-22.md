# DD-C-0081 verification — 2026-07-22

Result: passed as a `verified` bounded negative result.

Every fixed-budget cell compares the visible-action equilibrium with the same
full-credit Bayes rule using only the shared and own private clue. Visibility
strictly lowers both discovery and expected distinct actions in 18 of 32 cells,
ties in 14, and improves neither measure in any cell. Exact labeled enumeration
checks the visible policy trees and exact simple-policy enumeration checks the
history-hidden controls.

The result does not cover stopping feedback, equal-split rewards, assigned
roles, correlated recommendations, or other channels. It is retained as a
bounded null against the candidate visibility-improves-dispersion hypothesis.

Commands: `pytest tests/unit/test_dynamic_attention.py
tests/integration/test_dynamic_attention_cli.py`; `make validate-claims`.
