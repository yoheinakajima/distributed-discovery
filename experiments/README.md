# Experiments and runs

Reusable configurations live in `configs/`; immutable run manifests are indexed in `manifests/`. Full compact run directories live under the relevant study results or `results/` and follow `docs/reproducibility.md`.
