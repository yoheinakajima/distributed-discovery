# Statistical plan

The future base uses sealed task instances as paired clusters, eight separate
primary metrics, four architecture contrasts, 10,000 stratified paired
cluster-bootstrap replicates, Holm correction within metric/model families,
conservative missingness, fixed four-batch stopping, and no composite. Pilot
outputs are engineering-only and excluded.
