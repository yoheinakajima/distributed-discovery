# TreasureBench provider-schema conformance successful closeout

This living ExecPlan governs `AO-0005`. It supersedes `AO-0004` only for
public-safe closeout authority. It cannot rerun, append, reactivate, reuse,
rescore, or reauthorize any AO-0004 canary gate, authorization, or ledger.

## Purpose and intended outcome

Preserve the exact AO-0004 engineering history, reconcile the successful R4
provider-schema conformance outcome across public-safe repository surfaces,
accurately retitle and describe draft PR #199, pass the complete validation
wall, and—only after an exact committed owner gate—mark the PR ready,
squash-merge it, observe normal CI and Pages, verify public-safe live routes,
close issue #198, and synchronize `main`.

## Current state

Registration audit at `2026-07-27T23:00:00Z`:

- `AO-0005` is the next available Agent Operations identifier;
- issue #198, draft PR #199, and branch
  `benchmark/treasurebench-provider-schema-conformance` remain the bound
  GitHub objects;
- the registration head is
  `efe473a78fd0de17826d462fc685d2eecc2e77f0`;
- AO-0004 R4 executed at frozen commit
  `dcd89bca5daa54386df163439a010583c30134ec`;
- all four R4 canaries passed, both complete schemas passed provider and
  provider-independent exactly-one-final-action validation, and no bisection
  ran;
- R4 totals are 1,603 input tokens, 275 output tokens, USD 0.0086685 total,
  OpenAI USD 0.0031875, and Anthropic USD 0.005481;
- R1 and R2 are superseded and unused; R3 and R4 are consumed and terminal;
- every R1 through R4 gate, authorization, ledger, outcome, call, cost, and
  terminal decision is immutable;
- private and scientific state remained unchanged;
- no reconciliation, PR mutation, readiness, merge, issue closure, CI/Pages,
  live-route verification, or main synchronization is permitted before the
  exact AO-0005 gate.

## DISCUSSION AND DECISION DELTA AUDIT

The full program-memory registry was re-read. PM-0033 is newly due because its
`explicit-owner-decision` review trigger occurred when the owner accepted the
R4 success and selected a closeout. That due item is routed to this fixed
contract, this plan, the immutable R4 outcome, and the post-authorization
program-memory closeout update. PM-0031's wholly fresh identity rule remains
in force for the later candidate. PM-0014 and PM-0015 remain evidence-dependent
because public engineering conformance is not claim-grade evidence; PM-0001
through PM-0004 and PM-0010 remain deferred to their existing triggers. No
scientific, publication, performance, or private-evaluation trigger occurred.
The owner's later-pilot candidate is durably recorded here without registering
or authorizing it. No owner decision remains only in conversation.

Repeat this audit after authorized merge and live verification. At that point,
update PM-0033 to the completed conformance closeout and record only the
unregistered next candidate; do not allocate another task, campaign, or
private identity.

## Scope

1. Register AO-0005 on the existing issue, branch, PR, and session.
2. Freeze the closeout-only contract and exact public artifact identities.
3. Pass the complete authorization-free validation wall.
4. Commit and push the exact closeout execution surface.
5. Commit and push a generic zero-call, zero-spend owner-gate manifest and
   validate it without authorizing it.
6. Stop with a schema-valid owner-gate-required handoff.
7. After exact authorization, reconcile only public-safe closeout
   destinations and update PR #199's title and body.
8. Re-run the full validation wall, verify required checks, and only then mark
   PR #199 ready and squash-merge it.
9. Verify normal post-merge CI and Pages plus the named public-safe routes,
   close issue #198, synchronize `main`, repeat the decision-delta audit, and
   return the final schema-valid handoff.

## Non-goals

- No credential access, provider call, spend, canary, bisection, or provider
  execution.
- No R3 or R4 authorization or ledger read, append, mutation, reactivation,
  replacement, or reuse.
- No private-state read, write, generation, deletion, publication, pilot,
  campaign, batch, seed, task, answer, key, ciphertext, custody, or run.
- No DD-023, claim, scientific run, evidence or proof promotion, paper or PDF
  change, ranking, composite, leaderboard, or performance publication.
- No package, release, DOI, arXiv, journal, submission, upstream, or
  base-campaign action.
- No registration or execution of the later wholly fresh sealed
  repair-confirmation pilot.

## Assumptions

- The tracked R3 and R4 public artifacts are sufficient to verify exact
  preservation; local authorization contents and private state must not be
  inspected.
- Normal GitHub Actions and Pages triggered by an authorized squash merge are
  closeout infrastructure, not release-publication authority.
- PR title/body changes, readiness, merge, route observation, issue closure,
  and main synchronization require the exact AO-0005 authorization.
- The future wholly fresh pilot is only a candidate until a later task
  separately creates wholly new campaign, batch, CSPRNG seed, private tasks,
  answers, keys, ciphertexts, custody, and authorization.

## Milestones

- **M0 — complete:** select AO-0005, reconcile the owner decision, and freeze
  the closeout-only contract.
- **M1 — active:** pass authorization-free validation, commit and push the
  execution surface, commit and push the generic gate, validate it without
  authorizing, and stop with the owner-gate-required handoff.
- **M2 — pending:** validate the exact authorization and reconcile only the
  public-safe AO-0004 closeout destinations.
- **M3 — pending:** update PR #199, re-run the full validation wall and required
  checks, and mark it ready.
- **M4 — pending:** squash-merge, verify post-merge CI/Pages and public-safe
  routes, close issue #198, and synchronize `main`.
- **M5 — pending:** repeat the decision-delta audit and return the final
  schema-valid complete handoff.

## Progress checklist

- [x] Re-read root, planning, Agent Operations, benchmark, implementation,
  study, and site authority.
- [x] Confirm AO-0005 is the next identifier and preserve the existing
  issue/branch/PR/session.
- [x] Reconcile due program-memory decisions without creating scientific,
  publication, performance, or private authority.
- [x] Freeze all permissions false except bounded repository, GitHub, merge,
  CI/Pages, and public network observation.
- [x] Freeze zero calls, zero spend, no credential access, no provider action,
  no private access, no scientific mutation, and no release or submission.
- [x] Pass the complete authorization-free validation wall.
- [ ] Commit and push the exact execution surface.
- [ ] Commit, push, and live-validate the generic owner-gate manifest without
  creating an authorization.
- [ ] Stop with a schema-valid owner-gate-required handoff.
- [ ] Complete M2 through M5 only after exact owner authorization.

## Discoveries and surprises

- AO-0004 intentionally lacks merge and Pages authority, so its successful
  conformance result cannot close its own draft PR.
- Fixed-contract semantics require AO-0005 rather than broadening AO-0004
  after its gated execution.
- R4 prospectively resolves the bounded R3 128-token failure, but does not
  convert either execution into scientific or model-performance evidence.
- The tracked safe ledgers and outcomes are sufficient for closeout
  preservation; authorization contents, raw outputs, prompts, credentials,
  and private state are unnecessary and prohibited.
- The paper build refreshed two tracked Information Sharing Frontier
  provenance `source_commit` fields as a known build side effect. They were
  restored to their exact registered bytes; all 1,057 protected claim, run,
  paper/PDF, release, and AO-0004 files match the pre-validation baseline.
- The AO-0003 template named a nonexistent `docs/roadmap.md`; AO-0005 uses
  the canonical `docs/current-roadmap.md` and explicitly includes the existing
  project-status and final-handoff destinations.

## Decision log

- `2026-07-27T22:47:18Z`: preserve the terminal R4 decision
  `conformance-pass-both-complete-schemas`, four calls, exact token and cost
  totals, no bisection, cleared credentials, and unchanged private and
  scientific state.
- `2026-07-27T23:00:00Z`: accept the owner's successful-conformance closeout
  selection, allocate AO-0005, bind it to issue #198, draft PR #199, the
  existing branch and artifacts, and keep all calls and spend at zero.
- `2026-07-27T23:00:00Z`: record the later wholly fresh sealed
  repair-confirmation pilot registration as the next candidate without
  registering, authorizing, or executing it.
- `2026-07-27T23:45:11Z`: pass the complete authorization-free wall with 100
  focused provider-schema tests, 514 repository tests, strict MyPy over 192
  source files, 110 claims, 51 run manifests, seven papers totaling 119 pages,
  an 89-page/26-study site, and all governance, benchmark, release-readiness,
  Agent Operations, and program-memory audits. Restore the two generated
  provenance fields and confirm all 1,057 protected baseline files exact.
- `2026-07-27T23:50:00Z`: correct the inherited nonexistent roadmap scope to
  `docs/current-roadmap.md` and explicitly bind `reports/project-status.md`
  and `reports/final-handoff.md`; permissions, caps, and intended actions are
  unchanged.

## Validation strategy

Validate the fixed contract, ExecPlan, generic gate, and handoff against Agent
Operations schemas. Run Agent Operations and program-memory audits, all
TreasureBench Agents audits and rehearsals, provider-schema conformance
offline tests, the complete repository verification wall, every paper and the
site. Record and compare scientific inventories, paper/PDF hashes, release
state, and every protected R1 through R4 artifact. Validate the committed
owner gate in nonauthorizing mode against live issue, PR, branch, ancestry,
task-contract hash, protected trees, zero caps, permissions, prohibitions, and
expiry.

## Commands and expected observations

Before authorization:

    make bootstrap
    make audit-agent-ops
    make audit-program-memory
    make audit-publication-infrastructure
    make audit-treasurebench-naming
    make release-readiness
    make audit-agents-v1
    make audit-agents-v1-evaluation
    make agents-v1-dry-run
    make agents-v1-readiness
    make treasurebench-provider-schema-conformance-audit
    make verify
    make papers
    make site
    make owner-gate GATE=<committed-manifest> OWNER_GATE_VALIDATE_ONLY=1

Expected: every command exits zero; provider calls, credential reads,
authorization reads, private-state changes, spend, scientific mutations,
paper/PDF changes, releases, submissions, PR mutations, merge, issue closure,
and deployment remain zero. The protected R1 through R4 hashes remain exact.

Observed before the execution freeze: every command exited zero. The focused
provider-schema wall passed 100 tests; Ruff passed over 321 files; strict MyPy
passed over 192 source files; all 514 repository tests passed; 110 claims and
51 run manifests validated; seven papers totaling 119 pages and the
89-page/26-study site passed; and the dry-run compendium verified five assets
and seven papers. No credential or authorization was read, no provider or
private-state access occurred, calls and spend remained zero, and all 1,057
protected baseline files were byte-exact after restoring two known generated
provenance-only build side effects.

## Artifacts produced

- `tasks/treasurebench-provider-schema-conformance-closeout.yml`
- `plans/TREASUREBENCH_PROVIDER_SCHEMA_CONFORMANCE_CLOSEOUT.md`
- one exact committed generic AO-0005 owner-gate manifest
- one schema-valid owner-gate-required handoff

## Blockers

No implementation blocker is known. M1 must finish and then the exact AO-0005
generic owner authorization remains required before any closeout mutation.

## Recovery and restart instructions

Start from the exact pushed branch and PR head named by the AO-0005 handoff.
Read the fixed contract, this plan, committed gate, R4 outcome, R4 checkpoint
handoff, and every applicable scoped `AGENTS.md`. Do not inspect credentials,
local authorization contents, private state, raw provider outputs, or prompts.
Validate the exact authorization and every protected tree before M2. Stop and
replace the gate if an execution-sensitive file or bound identity changed.

## Outcome and retrospective

Pending. The intended outcome is a complete public-safe successful-conformance
closeout with every R1 through R4 artifact preserved, PR #199 merged only
after validation, and the later wholly fresh pilot left unregistered and
unauthorized.
