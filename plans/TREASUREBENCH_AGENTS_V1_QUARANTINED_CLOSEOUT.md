# TreasureBench Agents v1 quarantined closeout

This living ExecPlan governs `AO-0003`. It supersedes `AO-0002` only for
public-safe closeout authority. It cannot reopen, repair, access, execute, or
reauthorize the failed private-evaluation campaign.

## Purpose and intended outcome

Preserve the exact AO-0002 stop-by-policy outcome, reconcile public repository
status, accurately retitle and describe draft PR #197, pass the complete
closeout validation wall, and—only after an exact committed owner gate—mark the
PR ready, squash-merge it, observe normal CI and Pages, verify public-safe live
routes, close issue #196, and synchronize `main`.

## Current state

Registration audit at `2026-07-27`:

- the next available Agent Operations identifier is `AO-0003`;
- issue #196, draft PR #197, and branch
  `benchmark/treasurebench-agents-v1-fresh-pilot` remain the bound GitHub
  objects;
- the current branch and pull-request head at registration is
  `0c068b1a4ce40e81a871e4040a0e83825821ac20`;
- AO-0002 ended as `sealed-pilot-quarantined-provider-failure`;
- campaign `treasurebench-agents-v1-repair-confirmation-v1` and batch
  `tb-agents-v1-repair-confirmation-v1-b01` are permanently quarantined;
- execution commit `fe313602df7f4e8ffac1a1a02c2b3a83f3c72943`
  remains immutable;
- exactly one OpenAI public-canary HTTP 400 `schema-or-parameter` attempt
  occurred, Anthropic received zero calls, reported tokens are zero, and cost
  is USD 0.00;
- the provider phase is closed and the output lock is
  `sha256:8102a6c1b6bda003336d5503136dfe29301b04cb8f35e7740edd8d56f0eb3c1d`;
- the R2 authorization is inactive and archived;
- no task seed, task, answer, task key, answer key, task ciphertext, answer
  ciphertext, custody manifest, or private run was created;
- no credential or private-state access, provider call, spend, GitHub closeout
  mutation, merge, or deployment is permitted before the AO-0003 gate.
- the exact generic AO-0003 authorization passed at `2026-07-27T14:49:00Z`
  against execution commit
  `5cfac91cf2a47b6a00f6fe82c07d3ceeee21b188`, current PR ancestry, every
  protected tree, and the zero-dollar and zero-call caps;
- the authorization digest is
  `sha256:c5ec1bbdf1e2651bfa77c7902fe0a413de840b65251de407b5670af6c16716fd`
  and its local file mode is `0600`;
- no prior AO-0003 authorization was replaced and the gate engine performed
  no consequential action.

## DISCUSSION AND DECISION DELTA AUDIT

The program-memory registry was re-read. PM-0031 already preserves
the original quarantined-pilot and fresh-identity rule, and PM-0032 already
routes repository-native Agent Operations. The owner's newly adopted AO-0002
closeout disposition is routed to this fixed contract, this plan, the
public-safe stop artifacts, and PM-0033, whose canonical destination is the
separately unregistered next-task delta.
No evidence-dependent trigger occurred and no scientific or publication
decision is created. No owner decision remains only in conversation. Repeat
the audit after authorized external closeout and confirm the next candidate
remains unregistered.

## Scope

1. Register AO-0003 without creating new issue, branch, or PR identities.
2. Add a public-only validator for the immutable redacted AO-0002 closeout.
3. Freeze and commit a generic owner-gate manifest at an exact clean,
   pushed, current PR head.
4. Stop before closeout mutations and return the typed owner-gate-required
   handoff.
5. After exact authorization, reconcile public-safe documentation, task and
   plan status, program memory, DD-010, TreasureBench, site, and JSON surfaces.
6. Update PR #197 to the actual quarantined outcome, validate, mark ready,
   squash-merge, verify post-merge CI/Pages and public routes, close issue #196,
   and synchronize `main`.

## Non-goals

- No credential or private-state access, provider call, spend, generation,
  deletion, publication, or authorization recreation.
- No retry, repair, reopen, reuse, rescore, or execution of the failed
  campaign or batch.
- No provider request, schema, adapter, retry, error, or execution-code repair.
- No DD-023, claim, scientific run, evidence or proof promotion, paper or PDF
  change, ranking, composite, or performance publication.
- No package, release, DOI, arXiv, journal, submission, or base campaign.

## Assumptions

- The committed public closeout and output-lock commitment are sufficient to
  validate preservation; private state and credentials must not be inspected.
- Normal GitHub Actions and Pages triggered by an authorized squash merge are
  CI/Pages closeout, not release-publication authority.
- PR title/body changes, readiness, merge, route observation, issue closure,
  and main synchronization require the exact AO-0003 owner gate.
- The future schema-conformance task is only a candidate until separately
  registered and owner-authorized.

## Milestones

- **M0 — complete:** select AO-0003 and freeze the closeout-only contract.
- **M1 — complete:** implement public-safe validation, pass pre-gate acceptance,
  commit and push the execution surface, commit and push the exact generic
  owner-gate manifest, validate it without authorizing, and stop.
- **M2 — complete:** validate the local authorization and
  reconcile all public-safe closeout destinations.
- **M3 — active:** update PR #197, pass validation and CI, and mark it ready.
- **M4 — pending:** squash-merge, verify normal post-merge
  CI/Pages and live routes, close issue #196, synchronize main, and complete
  the final delta audit.

## Progress checklist

- [x] Re-read repository, Agent Operations, scoped benchmark, study, site, and
  planning authority.
- [x] Confirm the next task identifier and live GitHub bindings.
- [x] Freeze all public permissions false except bounded repository, GitHub,
  merge, CI/Pages, and network observation.
- [x] Freeze provider, spend, private, scientific, paper, performance,
  release, submission, and base-campaign authority false.
- [x] Pass the closeout-specific public artifact audit.
- [x] Pass documentation, scientific-invariant, paper, site, and Agent
  Operations validation.
- [x] Commit and push the exact pre-gate execution surface.
- [x] Commit, push, and validate the generic owner-gate manifest.
- [x] Stop with a schema-valid owner-gate-required handoff.
- [x] Create and validate the exact generic AO-0003 authorization.
- [x] Reconcile only the authorized public-safe documentation, DD-010,
  program-memory, TreasureBench site, and JSON surfaces.
- [x] Re-run the complete registered validation wall with all protected trees,
  scientific inventories, paper/PDF bytes, and release state unchanged.
- [ ] Complete M3 and M4 sequentially.

## Discoveries and surprises

- The AO-0002 fixed contract intentionally lacks merge and Pages authority, so
  it cannot legally close its own existing draft PR.
- Agent Operations fixed-contract semantics require a new task rather than
  expanding or editing AO-0002 after its execution.
- The public redacted reports contain enough exact commitments to validate the
  stop without reading the archived authorization or any private state.
- The full validation wall passed 448 tests, strict MyPy over 190 source files,
  110 claims, 51 run manifests, seven papers totaling 119 pages, an 89-page
  site covering 26 studies, the 500-run synthetic rehearsal, and all 54
  registered corruptions.
- The paper build refreshed two tracked provenance source-commit fields as a
  build side effect. They were restored byte-for-byte to their registered
  values; no paper or PDF change remains in the task diff.

## Decision log

- `2026-07-27`: accept `sealed-pilot-quarantined-provider-failure` as the
  permanent AO-0002 disposition.
- `2026-07-27`: allocate AO-0003 and bind it to issue #196, PR #197, the
  existing branch, and existing quarantined artifacts.
- `2026-07-27`: set the AO-0003 cost and call caps to zero and keep all
  provider, credential, private, scientific, paper, performance, release,
  submission, and base-campaign permissions false.
- `2026-07-27`: record `TreasureBench exact provider-schema conformance repair
  and public-canary gate` as the next substantive candidate, public-only and
  separately registered.
- `2026-07-27`: pass the complete pre-gate validation wall with zero provider
  calls, credential reads, private objects, spend, scientific mutation, or
  paper/PDF change.
- `2026-07-27T14:49:00Z`: validate and create the exact AO-0003 generic
  authorization; no provider, credential, private-state, scientific, paper,
  merge, or deployment action occurs in the gate engine.
- `2026-07-27T15:08:24Z`: complete M2 public-safe reconciliation and the full
  registered validation wall; preserve every protected tree, scientific
  inventory, paper/PDF byte, release state, zero-cap boundary, and AO-0002
  quarantine fact.

## Validation strategy

Validate the fixed contract and future task delta against Agent Operations
schemas. Run the dedicated redacted closeout audit. Run the infrastructure
acceptance profile, complete `make verify`, all papers, and the site. Confirm
scientific inventories and paper/PDF bytes remain unchanged from the
registration head. Validate the committed owner gate in nonauthorizing mode
against the live issue, PR, branch, ancestry, task-contract hash, protected
tree hashes, zero caps, permissions, prohibitions, expiry, and clean pushed
head.

## Commands and expected observations

Before authorization:

    make treasurebench-fresh-pilot-closeout-audit
    make bootstrap
    make audit-agent-ops
    make audit-program-memory
    make audit-publication-infrastructure
    make audit-treasurebench-naming
    make release-readiness
    make audit-agents-v1
    make audit-agents-v1-evaluation
    make agents-v1-dry-run
    make agents-v1-readiness
    make verify
    make papers
    make site
    make owner-gate GATE=<committed-manifest> OWNER_GATE_VALIDATE_ONLY=1

Expected: every command exits zero; provider calls, credential reads, private
state changes, spend, scientific mutations, paper/PDF changes, GitHub closeout
mutations, merge, and deployment remain zero.

Observed before the execution freeze: all commands exited zero; Ruff formatting
and lint passed, strict MyPy passed over 190 source files, 448 tests passed,
110 claims and 51 run manifests validated, the synthetic 500-run rehearsal and
54 corruptions passed, seven papers totaling 119 pages passed, and the site
passed at 89 pages and 26 studies. The closeout-specific audit preserved one
OpenAI call, zero Anthropic calls, zero reported tokens, USD 0.00, zero private
runs, the closed provider phase, and the exact output-lock commitment.

## Artifacts produced

- `tasks/treasurebench-agents-v1-quarantined-closeout.yml`
- `plans/TREASUREBENCH_AGENTS_V1_QUARANTINED_CLOSEOUT.md`
- `reports/agent-ops/next-task-treasurebench-provider-schema-conformance.yml`
- `scripts/audit_treasurebench_agents_v1_fresh_pilot_closeout.py`
- one exact committed generic AO-0003 owner-gate manifest
- one schema-valid owner-gate-required handoff

## Blockers

No implementation blocker is known. The exact AO-0003 authorization is active;
M2 is complete and M3 GitHub metadata, checks, and readiness are next.

## Recovery and restart instructions

Start from the exact pushed branch and PR head named by the AO-0003 handoff.
Read the fixed contract, this plan, committed gate manifest, existing redacted
AO-0002 closeout, and every applicable scoped `AGENTS.md`. Do not inspect
credentials, archived authorization contents, or private state. Validate the
exact authorization and protected tree before M2. Stop and replace the gate if
an execution-sensitive file or bound identity changed.

## Outcome and retrospective

Pending. The intended outcome is a complete public-safe quarantined closeout
with the failure permanently preserved and no transfer of authority to the
future provider-schema conformance candidate.
