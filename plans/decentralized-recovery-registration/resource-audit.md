# Resource audit

The exact counts and formulas are in
`reports/roadmap-consolidation/decentralized-recovery-state-space.{md,yml}`.
The immutable inherited slice has 16 channels, 225 ordered signal pairs, 207
positive-probability posterior rows, 46 distinct labeled posteriors, and 15
posterior-permutation classes.

A future exact audit would use rational arithmetic only, no network, no Monte
Carlo, expected wall time below two seconds and 64 MiB peak memory, with hard
caps of 30 seconds and 256 MiB. Because the gate stops, these caps authorize no
execution.
