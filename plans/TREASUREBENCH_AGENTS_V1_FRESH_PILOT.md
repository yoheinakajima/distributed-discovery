# TreasureBench Agents v1 fresh sealed repair-confirmation pilot

This living ExecPlan governs `AO-0002`, one DD-010 engineering-only private
evaluation. It registers and prepares a wholly fresh repaired TreasureBench
Agents v1 pilot, then stops at a committed exact Agent Operations owner gate.
It creates no scientific evidence.

## Purpose and intended outcome

Prepare a provider-current, fail-closed 50-task and 500-run repair-confirmation
pilot over five architectures, direct OpenAI and direct Anthropic, and one
repeat. Freeze a new campaign and batch, prove that the repaired instrument,
synthetic rehearsal, independent verification, custody design, corruptions,
budget accounting, and redaction boundaries pass, open one draft pull request,
and stop before credentials, private generation, custody creation, provider
calls, or spend with an exact owner-gate-required handoff.

## Current state

Live audit at `2026-07-27T02:04:22Z`:

- repository `yoheinakajima/distributed-discovery`;
- issue #196;
- branch `benchmark/treasurebench-agents-v1-fresh-pilot`;
- starting `main` and `origin/main`
  `b6934e6495282b210f0d90b96f63b24bebab454b`;
- no substantive pull request was open at registration;
- current-main CI `30145388340` and Pages `30145388319` passed;
- task ID `AO-0002`;
- contract `tasks/treasurebench-agents-v1-fresh-pilot.yml`;
- exact new campaign
  `treasurebench-agents-v1-repair-confirmation-v1`;
- exact new batch `tb-agents-v1-repair-confirmation-v1-b01`;
- 50 tasks, five architectures, two direct routes, one repeat, and 500 runs;
- provisional expected cost USD 15, hard cap USD 25, OpenAI cap USD 10,
  Anthropic cap USD 15, call cap 5,200, and cumulative spend USD 0;
- provider calls, credential reads, private generation, custody objects, owner
  authorization, and spend in this task are all zero;
- no DD-023, DD-C-0111, scientific run, paper result, ranking, composite,
  package, release, submission, or base campaign exists;
- the original pilot remains permanently quarantined and immutable;
- the five unrelated untracked preservation files retain their previously
  recorded SHA-256 values.

Exactly one milestone is active: M4.

## DISCUSSION AND DECISION DELTA AUDIT

The 32-record program-memory registry was read before issue and branch
creation. Every owner-adopted item has a canonical destination. No
evidence-dependent trigger occurred: this repair-confirmation pilot is
engineering-only, not claim-grade; the benchmark-paper, publication
recomposition, journal, retrievability, rename, bridge-theorem, and
local/open-model triggers remain unfulfilled. PM-0031 already routes the
quarantined-pilot and wholly fresh identity rule, and PM-0032 already routes
the generic Agent Operations workflow. Neither requires a new registry record.

The selected task delta is now routed to this fixed contract, this living plan,
issue #196, the fresh-pilot request and provider audit, and the future committed
owner-gate manifest. The full fresh option is no longer an unregistered
candidate, but registration does not create private, provider, spend,
scientific, paper, release, or base-campaign authority. No task-relevant owner
decision remains only in conversation after the registration commit. Repeat
this audit at closeout.

## Scope

1. Register issue #196, `AO-0002`, this branch, the fixed task contract, and
   this living plan.
2. Re-audit official provider availability, exact IDs, structured outputs,
   retention, retry/error behavior, and standard direct pricing.
3. Freeze a new public request and 50-slot allocation without generating any
   private task, answer, seed, key, or custody object.
4. Implement the fresh campaign as a separately parameterized path that uses
   the repaired one-final-action contract and the existing encrypted,
   resumable, output-lock-first execution primitives.
5. Run a public synthetic 50-case/500-run rehearsal and every registered
   identity, authorization, budget, custody, protocol, output-lock,
   contamination, redaction, and disclosure corruption.
6. Validate the private-evaluation profile and task-specific gates; open one
   draft PR.
7. Commit an exact generic Agent Operations owner-gate manifest binding the
   issue, PR, branch, commit, contract and execution-tree hashes, actions,
   cumulative state, caps, prohibitions, expiry, challenge, next milestone,
   and resume message.
8. Stop before all consequential actions and render the typed handoff.

## Non-goals

- No DD-023, claim, scientific run, evidence promotion, proof, paper result,
  ranking, composite, leaderboard, package, release, DOI, arXiv, journal, or
  submission.
- No base campaign, local/open model, OpenRouter route, fallback, or model
  substitution.
- No access to or reuse of the quarantined pilot, future-base identities,
  base-campaign identities, credentials, retained private state, or prior
  authorizations.
- No private seed, task, answer, key, or custody creation before the exact
  owner authorization.
- No provider call, account preflight, billed token, or spend before the exact
  owner authorization.

## Assumptions

- Repair confirmation requires route and pinned-model continuity. The
  `2026-07-27` official audit therefore retains
  `gpt-5.4-2026-03-05` and `claude-sonnet-4-6` even though newer model families
  exist.
- The direct OpenAI Responses and direct Anthropic Messages routes remain the
  only eligible routes; aliases, fallbacks, regional premiums, batch pricing,
  and provider-managed tools are excluded.
- The existing repaired schema, prompt, parser, orchestration, Method A/B,
  independent Method C, and metric-range checks remain the instrument source.
- Agent Operations authorization is necessary but never sufficient to perform
  a provider or private action; the resumed execution must also pass custody,
  route, budget, idempotency, and execution-tree checks.

## Milestones

- **M0 — complete:** registration, live baseline, provider audit, fixed contract,
  living plan, and first commit.
- **M1 — complete:** fresh request, allocation, provider record, identity
  separation, and budget/custody contracts.
- **M2 — complete:** offline fresh-campaign implementation and generic
  owner-authorization consumption boundary.
- **M3 — complete:** repaired synthetic 50-case/500-run rehearsal and all
  corruptions.
- **M4 — active:** focused and private-evaluation acceptance.
- **M5 — pending:** draft pull request, exact execution freeze, committed
  owner-gate manifest, gate validation, and typed handoff.
- **M6 — pending:** owner-authorized credentials, private generation, custody,
  provider execution, output lock, verification, redaction, and closeout.

## Progress checklist

- [x] Read root, planning, repository, Agent Operations, scoped benchmark,
  scoped implementation, study, reproducibility, and claim-status authority.
- [x] Audit Git, GitHub, current CI/Pages, scientific ceilings, and unrelated
  preservation files.
- [x] Complete the registration discussion and decision delta audit.
- [x] Allocate `AO-0002` and create issue #196 and the task branch.
- [x] Re-audit current official provider documentation without credentials or
  provider API calls.
- [x] Commit the fixed task contract and living ExecPlan.
- [x] Complete M1 through M3 sequentially.
- [ ] Complete M4 and M5 sequentially.
- [ ] Stop with the exact owner-gate-required handoff.

## Discoveries and surprises

- OpenAI's current flagship guidance has moved beyond GPT-5.4, but the exact
  `gpt-5.4-2026-03-05` snapshot remains listed for Responses, structured
  outputs, and data-residency support. Model continuity is therefore feasible.
- Anthropic now documents that dateless 4.6 IDs are canonical pinned snapshots,
  not moving aliases. `claude-sonnet-4-6` remains suitable for exact continuity.
- Anthropic's current retention documentation says ordinary Messages content
  is not retained by default; structured-output schemas alone may be cached for
  up to 24 hours. OpenAI `store: false` suppresses Responses application state
  but does not itself create Zero Data Retention; default abuse-monitoring
  retention may be up to 30 days.
- The generic Agent Operations owner-gate CLI already accepts a noninteractive
  challenge, but the Make target did not expose that argument. M2 added a
  generic Make variable rather than a task-specific authorization helper.
- A tracked gate manifest cannot contain the SHA-1 of the commit that contains
  itself. The generic engine now binds the prior exact execution commit,
  verifies it is an ancestor of the clean local/remote/live-PR manifest head,
  and rehashes every declared execution-sensitive tree. The zero-distance case
  remains valid, and descendant, nonancestor, stale-head, and changed-tree
  regressions are explicit.
- The old generic prohibition `credential-read` conflicted with a gate whose
  declared private action is an exact credential read. It is now
  `credential-read-outside-manifest`; undeclared private access remains
  prohibited and every declared action must still map to a true fixed-contract
  permission.

## Decision log

- `2026-07-27T02:04:22Z`: allocate `AO-0002`, issue #196, branch
  `benchmark/treasurebench-agents-v1-fresh-pilot`, campaign
  `treasurebench-agents-v1-repair-confirmation-v1`, and batch
  `tb-agents-v1-repair-confirmation-v1-b01`.
- `2026-07-27T02:04:22Z`: retain the original two pinned model IDs because the
  task is repair confirmation; exclude newer models, aliases, batch routes,
  fallback routes, provider tools, and regional-price variants.
- `2026-07-27T02:04:22Z`: freeze authorization-capable permissions in the fixed
  contract, while requiring the generic exact-commit gate plus the runtime's
  independent fail-closed checks before any consequential action.
- `2026-07-27T02:08:16Z`: the fixed contract passed its JSON Schema, all
  required ExecPlan sections are present in the mandated order, and
  `git diff --check` passed. Advance from M0 to M1.
- `2026-07-27T02:23:29Z`: freeze the public 50-slot allocation recipe and
  exact request; record the current official provider audit; implement generic
  authorization validation plus gated fresh generation; parameterize custody
  associated data without changing historical defaults; pass the 50-case,
  500-run, 3,014-turn repaired rehearsal, all 28 repaired-instrument
  corruptions, and all 18 fresh identity/gate/custody corruptions with zero
  credentials, private objects, network, provider calls, or spend. Advance
  through M1, M2, and M3 to M4.

## Validation strategy

M0 validates the task contract, plan structure, issue/branch identity, and
tracked diff. M1 adds JSON Schema checks and independent identity/budget
reconciliation. M2 adds authorization, execution-tree, custody, idempotency,
resume, and no-provider-in-CI tests. M3 runs the complete deterministic
rehearsal and injects every required corruption. M4 runs:

```sh
git diff --check
make bootstrap
make audit-agent-ops
make audit-agents-v1
make audit-agents-v1-evaluation
make agents-v1-dry-run
make agents-v1-readiness
make treasurebench-fresh-pilot-audit
make treasurebench-fresh-pilot-rehearsal
make verify
```

M5 repeats focused acceptance from a clean pushed draft-PR head, verifies the
gate manifest against exact local/remote/PR observations without creating an
authorization, renders the typed handoff, and rechecks the scientific,
release, private, provider, spend, and preservation boundaries.

## Commands and expected observations

- `make audit-agent-ops`: both task contracts, the owner-gate manifest, and all
  Agent Operations schemas and corruptions pass.
- `make treasurebench-fresh-pilot-audit`: exact campaign, batch, routes,
  models, counts, budgets, identity exclusions, custody contract, repaired
  protocol, and corruptions pass with zero private/provider activity.
- `make treasurebench-fresh-pilot-rehearsal`: 50 public synthetic cases and
  500 synthetic architecture/model runs pass Method A/B/C, range, lock,
  contamination, redaction, and corruption checks with zero network calls.
- `make owner-gate GATE=... OWNER_GATE_CHALLENGE='AUTHORIZE ...'`: after the
  owner supplies the exact challenge, the generic engine writes one mode-0600
  authorization outside Git and performs no consequential action.

## Artifacts produced

At M0: issue #196, branch
`benchmark/treasurebench-agents-v1-fresh-pilot`, fixed contract
`tasks/treasurebench-agents-v1-fresh-pilot.yml`, and this living ExecPlan.
The contract passed `task-contract.schema.json`; the plan has all 15 required
sections and the discussion/delta audit immediately follows current state. M1
through M3 add the fresh request and allocation recipe, official-provider
audit, registration record, offline module and audit target, generic gate
hardening, the passing 500-run rehearsal, and the 46-corruption record.

## Blockers

No registration blocker. Credentials, private generation, custody creation,
provider calls, and spend are intentionally blocked until M5 commits and
validates the exact owner-gate manifest and the owner creates its local
authorization.

## Recovery and restart instructions

Inspect `git status --short --branch`; preserve the five named unrelated
untracked files; read the root and scoped instructions, fixed contract, this
plan, issue #196, and the single draft PR observed for this branch. Resume the
only active milestone. Never read credentials, retained pilot state, or local
owner authorizations during offline work. Never generate private material or
call a provider before the exact gate.

## Outcome and retrospective

Pending. Complete registration-through-gate only after M0 through M5 pass and
the handoff is `owner-gate-required`. M6 remains unstarted until the owner
authorizes the exact manifest.
