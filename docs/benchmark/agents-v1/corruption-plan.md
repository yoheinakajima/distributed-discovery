# Corruption plan

Version `agents-corruption-v1` registers 24 mutations spanning prompt and
capability leakage, version/commitment integrity, action and protocol validity,
model snapshot identity, trace/redaction safety, exact baselines and metric
conventions, contamination probes, compatibility exclusions, and the
no-composite rule.

Every record specifies the exact mutation, rejecting component, expected error
class, and scientific or governance purpose. This registration validates the
registry and public toy fixtures only. It does not corrupt model outputs or
scientific results because neither exists.
