# Foundations of Distributed Discovery — DD-000

This placeholder is deliberately limited to the charter-level seed until M2 validates notation against pinned upstream and the literature. No general theorem is claimed here.

A provisional problem tuple is \(D=(\Omega,\mu,E,I,A,\Gamma,B,\tau,R)\). A discovery architecture is provisionally \(H=(I,\Pi,R)\), with a protocol mapping information histories to distributions over joint actions. The Information–Action Decomposition \(G_B(\Pi;I)=V_B(I)-L_B(\Pi;I)\) is an accounting identity by definition of protocol loss, not yet a stronger theorem.
