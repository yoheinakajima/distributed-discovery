# Compatibility entry point

Read, in order: `AGENTS.md`, `.agent/PLANS.md`, `plans/MASTER_EXEC_PLAN.md`, `docs/repository-contract.md`, then the nearest study-specific `README.md`, `plan.md`, and `status.yml`. Those files are authoritative; this file intentionally does not duplicate them.
