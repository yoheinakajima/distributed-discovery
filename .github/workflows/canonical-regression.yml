name: Canonical baseline regression
on:
  workflow_dispatch:
  schedule:
    - cron: "17 4 1 * *"
jobs:
  reproduce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: uv python install 3.12
      - run: make bootstrap
      - run: make reproduce-baseline
