# Site tests

`distributed_discovery.site.build.validate_site` checks internal links and fragments, semantic landmarks and heading order, unresolved data markers, page count, and tracking absence. `tests/integration/test_site_build.py` builds into an isolated temporary directory and confirms generated benchmark and registry content.
