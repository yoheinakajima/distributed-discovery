# Private companion site

M4 will create a local, dependency-light companion after inspecting the pinned upstream visual language. It must remain private, un-deployed, accessible, responsive, tracker-free, and use generated numerical data.
