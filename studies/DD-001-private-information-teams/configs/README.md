# DD-001 configurations

`initial-grid.yml` records the validated 21-case tiny grid and bounded canonical coordinate search. `signature-certificate.yml` independently audits the lossless signature reduction, feasibility theorem, policy reconstruction, prior tiny optima/ties, and canonical state-space cost. `alignment-upper-bound.yml` registers the exact joint-column global-count relaxation, its benchmark sources, resource limits, and certificate requirements. Every configuration enforces explicit time and enumeration limits.
