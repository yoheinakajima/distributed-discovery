# Minimum viable model

Replace the indicator box objective with a world-indexed monotone set or multiset value \(v_\theta(S)\). Begin with finite deterministic coverage sets before submodular generalization.
