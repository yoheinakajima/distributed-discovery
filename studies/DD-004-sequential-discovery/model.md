# Minimum viable model

Actions follow \(A_t=\Pi_t(H_t)\), where history contains prior actions and observable outcomes. Observation timing and whether failure eliminates candidates must be explicit.
