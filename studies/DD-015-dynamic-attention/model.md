# Registered model boundary

The candidate scope is `M=3`, `N in {2,3}`, one shared clue, private clues,
sequential actions, and visible prior actions. Success-stopping and fixed-budget
objectives must be registered separately. Exact dynamic programming and a tiny
policy-tree enumerator are required before any result claim.
