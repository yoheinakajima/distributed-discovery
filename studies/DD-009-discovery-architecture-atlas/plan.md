# Executed plan

Classify all 288 Cartesian cells with deterministic validity rules, evaluate the
20 valid cells by exact state enumeration, reproduce every row with a separate
implementation, and reject a corrupted transfer record. Compute the declared
six-objective bounded dominance relation, preserve manifests and checksums, and
publish the registry, exact rows, claim, report, study page, and Atlas Lab data.
