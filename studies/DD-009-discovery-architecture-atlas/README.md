# DD-009 — Discovery Architecture Atlas

This bounded synthetic atlas aligns evidence, disclosure, allocation, timing,
and reward choices in one exact two-agent environment. It preserves invalid
Cartesian cells with explicit reasons instead of treating every combination as
coherent.
