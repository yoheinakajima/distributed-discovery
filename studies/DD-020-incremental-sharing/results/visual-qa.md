# DD-020 figure visual QA

- Reviewed: `2026-07-22`
- Artifact: `incremental-profiles.svg`
- Renderer: macOS Quick Look PNG at 1,000 pixels
- Scope: full figure, axes, five lines, selected markers, labels, and legend text

The first render exposed a collision between the guaranteed-shortlist label and
the confidence-point line. The two annotations were repositioned and the final
render shows complete axes, exact-profile lines, readable labels, no clipped
content, and a visible shared `G_1=7/8` point for the same-accuracy comparison.
The adjacent CSV remains authoritative for exact values.

