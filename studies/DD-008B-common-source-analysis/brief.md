# Brief

DD-008B proves a general finite-`N` threshold theorem for the frozen common-
source game. The all-common trap exists on an exact cost interval of width
`p(1-p)/N`. It also records an exact `N=3` interior counterexample showing that
private prize competition can produce more independent sources than the
planner, so the paper must not claim universal under-acquisition.
