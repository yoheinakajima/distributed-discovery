# Model

`C` is a free common source: two users observe the same draw. `I` costs `c`: each user observes an independent draw conditional on the target. Successful agents split a unit prize equally; social value is union discovery less source costs.
