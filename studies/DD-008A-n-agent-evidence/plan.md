# Plan

Derive exact discovery and source-contingent equal-split payoffs by binomial
sums; check them by direct target/signal enumeration. Classify pure source-count
equilibria and planner optima on a registered rational grid, with explicit ties,
then compare a bounded set of verification-dependent interventions. An immutable
run is created only after the implementation and independent verifier agree.

The primary run must be clean and record hashes of the configuration and both
independent evaluators. The earlier dirty registration checkpoint is retained as
non-primary provenance and excluded from claims.
