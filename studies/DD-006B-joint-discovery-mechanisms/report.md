# DD-006B report

Primary clean run `20260721T165512Z_DD-006B_f022a1a5_3be21d0b9b` enumerates 60
registered regime/coefficient rows and checks 57,600 unilateral joint
report/action-rule deviations with exact `Fraction` arithmetic. A separately
implemented evaluator reproduces every deviation, accounting, discovery, and
fixed-tie certificate and rejects corrupted records.

Six target-and-actions rows, four success-and-actions rows, and six sole-rescue
rows strictly implement truthful reporting and the declared differentiated
recommendation for both fixed tie roles. Three strict rows use an active
positive proper-score component. The largest all-tie strict margin is `13/72`,
attained in the target-and-actions regime at coefficients `(1/4,0,3/4)`.
Truthful differentiated discovery is `11/12`. The target-observable but
action-hidden regime has no weak row; its largest exact joint margin is
`-7/36`.

All 60 truthful profiles satisfy interim participation in the registered
normalization. Among strict rows, the minimum worst-case absolute transfer is
`1/2` and the minimum expected external subsidy is `1`. These are transfers in
addition to the equal-split prize. No row is claimed to be ex-post balanced.
Strict rows have a unique unilateral best response against the truthful profile,
but the run does not claim that the full game has a unique equilibrium.

DD-006's nine score-difference rows and DD-006A's 123 normalized-linear rows had
no strict implementation; DD-006B establishes a positive result only for this
registered subsidized class. It is not an arbitrary-mechanism theorem, a
dominant-strategy or collusion result, or evidence about a real institution.
