# Registered plan

Derive discovery and type-payoff identities; implement a closed-form binomial
evaluator and a materially separate target/signal enumerator; classify pure
attention equilibria and planner optima on a frozen rational grid; evaluate a
bounded observable reward family; preserve an immutable run; independently
verify its certificate and corruption rejection; then audit any claims.

No computation is authorized until a configuration records exact grid and
profile counts, runtime and memory estimates, caps, certificate format, and
valid partial outcomes.

The gate is now satisfied by `configs/census.yml`: 175 cells, 1,050 attention
profiles, 6,300 strategic reward/profile rows, 3,319,200 direct
action-relevant labeled states, a ten-minute/2-GB hard cap, exact fractions,
and no valid partial claim outcome. The next sequential steps are implementation
validation, a clean source commit, the single primary run, evidence audit,
public integration, and repository-wide acceptance.
