# DD-014 report

Primary clean run `20260721T222047Z_DD-014_f5f099a8_ea0276dd16` passed in
10.761 seconds, below the registered 120-second estimate and 300-second cap.
It evaluates 75 exact `(N,p,q)` cells and all 775 anonymous profiles of the
three registered conditional policy types.

For `p>=1/3`, replacing a third-option contrarian with a private-dominant role
weakly improves discovery, strictly when `p>1/3`. Conditional policies therefore
do not improve the DD-012 planner optimum. Contrarians occur in an optimal
profile in 15 grid cells, exactly those with uninformative private clues; they
tie rather than improve discovery.

The equal-split game has 115 weak and 53 strict equilibrium profiles. The best
weak equilibrium remains below the planner optimum in 23 of 75 cells, while
all weak equilibria are planner-optimal in 49 cells. Conditional action rules
can eliminate the incentive wedge in some cells but not in general.

The larger two-label audit exhausts all 1,024 ordered profiles of the sixteen
raw deterministic tables across four cells. Complementary constant policies
achieve discovery one and beat the embedded private/public optimum by `1/16`,
`1/12`, or `1/9`. This preserved negative scope result prevents an unrestricted
interpretation of the three-label theorem.

Independent table evaluators reproduce 346,275 main and 16,384 raw labeled
states. Altered discovery, payoff, equilibrium, and raw-optimum records are all
rejected. DD-C-0066 is verified; DD-C-0067 and DD-C-0068 are independently
reproduced, with the latter classified as a negative result.
