# Project status — DD-000

Milestones M0 through M3 are complete and M4, the private companion site extension, is active. The canonical upstream remains pinned, cached, and clean; the M3 additive paper patch applies and compiles reproducibly. See `plans/MASTER_EXEC_PLAN.md` for commands, blockers, and the exact resume point. M9 will replace this checkpoint with the full handoff report.
