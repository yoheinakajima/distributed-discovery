# AO-0007 AO-0006 custody diagnostic and prospective repair closeout

AO-0007 executed its owner-authorized diagnostic exactly once. The retained
AO-0006 output lock, exact inventory, eight locked objects, execution identity,
append-only access and usage/cost logs, provider-stage state, redacted summary,
and complete before/after filesystem snapshots verified. The diagnostic read
no seed or key value, task, answer, raw provider output, credential, private
host path, or unrelated object. It made no provider call, spent USD 0, and
mutated no retained AO-0006 state. Its one-use private-read authority is
permanently closed.

The exact engineering failure occurred at private task generation:
`treasurebench-agents-v1-repair-confirmation-v2` was absent from the
fail-closed private-generation campaign allowlist. This confirms the
public-source candidate without changing AO-0006's permanent terminal
decision or any retained object.

The prospective repair admits only the already frozen v2 campaign in that
generation gate. It also closes the two independently registered custody
conformance gaps: new custody objects use atomic exclusive creation, and a
resumed sealed object must decrypt to the newly requested plaintext rather
than matching only ciphertext and associated-data commitments.

The production-path live-mode-equivalent command passes all twenty registered
classes with 50 synthetic nonsecret tasks and answers in an isolated temporary
state root. An independent AES-256-GCM verifier confirms ciphertexts,
domain-separated associated data, unique nonces, commitments, manifest,
permissions, and reload. Wrong keys, corrupted ciphertext, symlinks, duplicate
mismatch, and existing-target creation reject; interrupted state is isolated;
temporary state is deterministically removed.

AO-0006 remains permanently quarantined with its exact campaign, batch,
execution commit, two public canaries, 1,349 input tokens, 253 output tokens,
USD 0.0076095 cost, zero private runs, no task or answer ciphertext, no custody
manifest, no unsealing, and output lock
`sha256:127a9c796459c7627f6fd90b92ef1587ad0f6b1910b4ff255c2ceb976f3ab25f`.
Nothing was retried, reopened, reused, rescored, spliced, repaired in place,
unsealed, or reauthorized.

This is engineering diagnosis and prospective offline conformance only. It is
not scientific or model-performance evidence and creates no new private pilot,
v3 identity, DD-023, claim, run, evidence or proof promotion, paper result,
ranking, composite, release, submission, or base-campaign authority.
