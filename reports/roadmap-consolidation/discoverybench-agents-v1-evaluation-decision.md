# DiscoveryBench Agents v1 evaluation decision

Decision: `sealed-pilot-ready-owner-authorization-pending`.

The exact OpenAI and Anthropic snapshots, 50-task sealed engineering pilot,
five architectures, one repeat, exact comparators, custody, trace,
authorization, and verification plans are registered. The pilot is
engineering-only. Estimated cloud cost is $44.832; the recommended hard cap is
$100, but cost authorization and execution permission are false.

The claim-grade base remains frozen as a 200-task, four-batch, three-repeat
design with all 138 cells and 62 boundary repeats. It is blocked because this
host cannot run the exact open/local candidate. No private material, provider
call, local invocation, download, trace, performance result, scientific claim,
immutable run, or DD-023 exists.

The owner action required is a separate explicit attestation that activates an
authorization for the exact commit and pilot tier, supplies positive total and
provider caps, expiration, batch IDs, and accepts custody and trace policies.
Until then every execution guard remains closed.
