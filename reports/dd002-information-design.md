# DD-002 bounded information-design result

DD-002 completely enumerates the declared \(M=3,N=2\), four-evidence-state deterministic-disclosure fixture. The 15 canonical partitions induce 37 posterior games and 256 global pure-equilibrium selections. All are stored exactly, together with the required anonymous symmetric equilibrium and the two-action planner benchmark.

A strict refinement from pooling to blocks \(\{0,1\}\), \(\{2,3\}\) lowers anonymous-symmetric discovery from \(5/9\) to \(171/308\). The deterministic map sending both fine messages to the pooling message is the Blackwell garbling witness. The same refinement raises every pure-equilibrium and planner outcome from \(2/3\) to \(3/4\), so the harm is entirely selection-dependent.

The complete 45-pair refinement census contains one anonymous-symmetric reversal, eight worst-pure reversals, no best-pure reversals, and no planner reversal. These are verified bounded facts, not general monotonicity theorems. Randomized disclosure and general asymmetric mixed-equilibrium classification remain outside the executed fixture.
